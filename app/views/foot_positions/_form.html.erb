<%= form_with(model: foot_position) do |form| %>
  <% if foot_position.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(foot_position.errors.count, "error") %> prohibited this foot_position from being saved:</h2>

      <ul>
        <% foot_position.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>

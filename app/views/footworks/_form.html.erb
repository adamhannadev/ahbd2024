<%= form_with(model: footwork) do |form| %>
  <% if footwork.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(footwork.errors.count, "error") %> prohibited this footwork from being saved:</h2>

      <ul>
        <% footwork.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :footwork, style: "display: block" %>
    <%= form.text_field :footwork %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
